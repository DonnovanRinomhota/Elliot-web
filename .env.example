# Chat/lead integration mode: "demo" (default, no backend required) or "live"
NEXT_PUBLIC_ELLIOT_MODE=demo

# Base URL of the real Elliot SaaS API. Only required when MODE=live.
# Expected endpoints: POST {url}/v1/website-chat, POST {url}/v1/leads/demo-request
NEXT_PUBLIC_ELLIOT_API_URL=
